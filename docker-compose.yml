services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: zona
      POSTGRES_PASSWORD: zona
      POSTGRES_DB: zona
    ports:
      - "5432:5432"
    volumes:
      - zona_pg:/var/lib/postgresql/data

volumes:
  zona_pg:
